project.name=dorado
project.version=1.0

project.build.dir=${basedir}/build
project.dist.dir=${basedir}/dist
project.temp.dir=${basedir}/temp
project.repository.dir=${basedir}/repository

project.tomcat.version=5.5.10
project.wrapper.version=3.1.2

depedencies.config=ROOT/depedencies.xml
izpack.home=${project.repository.dir}/izpack/izpack-src/_dist